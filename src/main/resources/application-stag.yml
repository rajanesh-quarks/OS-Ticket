spring:
  application:
    name: GControl-ticket-management-service

server:
  port: 8290
eureka:
  instance:
    hostname: https://192.168.1.37
    serviceUrl:
      defaultZone : http://${eureka.instance.hostname}:8761/eureka/


